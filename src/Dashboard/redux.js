export const types = {

}

export const actions = {

}

const initialState = {
    basicData: 'arr'
};
const History = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default History
